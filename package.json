{
  "name": "client-management-blockchain",
  "version": "1.0.0",
  "description": "A Client Management in Blockchain",
  "main": "index.js",
  "private": true,
  "scripts": {
    "init:infra": "./infra/init.sh",
    "down:infra": "./infra/down.sh",
    "start:dev": "lerna run --parallel --stream dev --scope client-manager-api --scope blockchain-service",
    "dev": "lerna run --parallel --stream dev --scope client-manager-api --scope client-manager-ui --scope blockchain-service",
    "sc": "lerna run --scope smart-contracts compile",
    "bs:download": "lerna run --scope blockchain-service --stream download",
    "bs": "lerna run --scope blockchain-service --stream dev",
    "api": "lerna run --scope client-manager-api --stream dev",
    "ui": "lerna run --scope client-manager-ui --stream dev",
    "api:test": "lerna run --scope client-manager-api test",
    "sc:test": "lerna run --scope smart-contracts test",
    "sc:compile": "lerna run --scope smart-contracts compile",
    "sc:deploy:client:core": "lerna run --scope smart-contracts deploy-client-core",
    "sc:upgrade:client:core": "lerna run --scope smart-contracts upgrade-client-core",
    "sc:deploy:points:core": "lerna run --scope smart-contracts deploy-points-core",
    "sc:upgrade:points:core": "lerna run --scope smart-contracts upgrade-points-core",
    "sc:clean": "lerna run --scope smart-contracts cache",
    "clean": "lerna run clean",
    "clear": "rm -rf node_modules && rm -rf packages/client-manager-ui/node_modules && rm -rf packages/client-manager-api/node_modules && rm -rf packages/smart-contracts/node_modules", 
    "build": "pnpm recursive run build",
    "test": "pnpm recursive run test"
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "pnpm": {
    "packageExtensions": {
      "posix": {
        "optionalDependencies": {
          "node-gyp": "*"
        }
      }
    }
  }
}
